Bu fayl şifrəlidir.
Deşifrə etmək üçün base64 decode istifadə et.

Terminal ilə:
  base64 -d secret.txt

Python ilə:
  python3 -c "import base64; print(base64.b64decode(open('secret.txt').read().strip()).decode())"

Nəticə sənə növbəti addımı göstərəcək.
Həmin URL-ə get - orada flag-i tapacaqsan!
